console.log("DOM manipulation script loaded.");

const h = document. getElementById('heading'); //first tag
console.log(h);

const p = document.getElementsByClassName('movie'); //first tag
console.log(p);

const h2= document.querySelector('h');
console.log(h);
const movie= document.querySelectorAll('.movie');
console.log(movie);